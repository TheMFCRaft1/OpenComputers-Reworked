package li.cil.oc.client

/** Client sound stub for shared tile entity traits. */
object Sound {
  def startLoop(tileEntity: Any, sound: String, volume: Float, delay: Int): Unit = ()
  def stopLoop(tileEntity: Any): Unit = ()
}
